Merge-gate review: one correction cycle is required before this can merge.

1. **Blocking — the new API-server wake path has an undeclared runtime dependency.** `gateway/wake.py:124` imports `aiohttp`, but `aiohttp` is available only through optional extras such as `messaging`; it is not in the base or `dev` install. In a clean `uv sync --extra dev --frozen` environment, `tests/gateway/test_wake_delivery.py` fails 6/10 tests with `ModuleNotFoundError: No module named 'aiohttp'`. The same failure is reachable in production for a base install using a stateless `api_server` subscription. Prefer the already-installed HTTP client rather than widening the base dependency, and make the wake tests run in the canonical clean environment.

2. **Blocking — completed-task artifacts are still not item-safe/retryable.** `gateway/kanban_watchers.py:1619-1645` catches artifact upload exceptions, ignores unsuccessful `SendResult` values, and returns success. The caller at `gateway/kanban_watchers.py:1327-1341` then acknowledges the event and advances the subscription cursor. A transient image/document/video failure therefore permanently loses that artifact, contrary to the source-card requirement that each item/chunk be durably acknowledged and that only the unsent tail retry. Persist deterministic delivery items for the image batch and each remaining artifact, use stable delivery keys, propagate failed sends, and add a fail-on-item-N/retry test proving acknowledged artifact prefixes are not replayed while failed tails are retained.

3. **Precondition — exact-head CI is absent and the branch is stale.** Exact head `9474de8f45fc3a6b4db34cc0f793e0f3ec1e122e` currently has zero check runs and zero commit statuses, so the review card's no-waiver CI gate cannot pass. The branch is also 39 commits behind live `main`, with overlapping changes in `tui_gateway/server.py`. Bring the same PR branch current with upstream, preserve the latest upstream behavior, and ensure applicable checks report green on the corrected exact head.

Local verification: the six focused notifier/wake files pass 78/78 only after manually installing optional `aiohttp`; `git diff --check` is clean. Please keep the correction narrowly scoped to this PR and report the corrected head plus exact commands/results.